server:
  address:     ":8006"
  openapiPath: "/api.json"
  swaggerPath: "/swagger"

logger:
  level: "all"
  stdout: true

database:
  default:
    link: "pgsql:postgres:654321@tcp(127.0.0.1:5432)/my_ad?sslmode=disable"

security:
  admin_token: "change-me"
